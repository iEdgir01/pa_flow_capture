# Build plan

## Phase 0 — Context (governance)
- [x] AGENTS.md + ai-context/ scaffolded per claude-global (2026-07-09)
- [x] Real content written from repo survey (README, manifest, source files, git log, published-versions/) — replaces auto-generated placeholders (2026-07-09)

## Phase 1 — v1.0.1 (shipped)
- [x] Initial extension: canvas detection, anchor placement, tile capture,
      OffscreenCanvas stitching, seam matching, auto-zoom (`967536d`)
- [x] AMO submission hardening: gecko data collection permissions
      (`6d72a04`), data consent schema fix (`1359bac`), unsafe innerHTML
      removal in precision cursor (`47b5e59`), icon/dwell-copy cleanup
      (`1c7c1d2`, `62f2bed`, `49e4046`)

## Phase 2 — Next milestone
- [ ] pending — no scope defined yet; next session should get task from user
      (bug fix against a real PA flow, new capture feature, or next release
      packaging).
